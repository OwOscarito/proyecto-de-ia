SECRET_KEY="secret key"
FRONTEND_PORT=8800

BACKEND_HOST=http://127.0.0.1
BACKEND_PORT=8888

# Solo necesario si BACKEND_HOST no es alcanzable por el navegador del
# usuario (ej. en Docker Compose, donde BACKEND_HOST es el nombre interno
# del servicio "back"). Si no se define, se usa BACKEND_HOST:BACKEND_PORT.
# BACKEND_PUBLIC_URL=http://localhost:8888
